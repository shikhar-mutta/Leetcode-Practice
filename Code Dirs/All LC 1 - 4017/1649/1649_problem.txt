══════════════════════════════════════════════════════════════════════════
 1649. Create Sorted Array through Instructions
 Hard  |  Tags: Array, Binary Search, Divide and Conquer, Binary Indexed Tree, Segment Tree, Merge Sort, Ordered Set
 https://leetcode.com/problems/create-sorted-array-through-instructions/description/
══════════════════════════════════════════════════════════════════════════

PROBLEM
  Given an integer array instructions, you are asked to create a sorted
  array from the elements in instructions. You start with an empty
  container nums. For each element from left to right in instructions,
  insert it into nums. The cost of each insertion is the minimum of the
  following:



  - The number of elements currently in nums that are strictly less than
    instructions[i].

  - The number of elements currently in nums that are strictly greater
    than instructions[i].




  For example, if inserting element 3 into nums = [1,2,3,5], the cost of
  insertion is min(2, 1) (elements 1 and 2 are less than 3, element 5 is
  greater than 3) and nums will become [1,2,3,3,5].


  Return the total cost to insert all elements from instructions into
  nums. Since the answer may be large, return it modulo 10^9 + 7

CONSTRAINTS
  1 <= instructions.length <= 10^5
  1 <= instructions[i] <= 10^5

TEST CASES
  [1] Input    : instructions = [1,5,6,2]
      Expected : 1
      Why      : Begin with nums = [].
                 Insert 1 with cost min(0, 0) = 0, now nums = [1].
                 Insert 5 with cost min(1, 0) = 0, now nums = [1,5].
                 … (3 more)

  [2] Input    : instructions = [1,2,3,6,5,4]
      Expected : 3
      Why      : Begin with nums = [].
                 Insert 1 with cost min(0, 0) = 0, now nums = [1].
                 Insert 2 with cost min(1, 0) = 0, now nums = [1,2].
                 … (5 more)

  [3] Input    : instructions = [1,3,3,3,2,4,2,1,2]
      Expected : 4
      Why      : Begin with nums = [].
                 Insert 1 with cost min(0, 0) = 0, now nums = [1].
                 Insert 3 with cost min(1, 0) = 0, now nums = [1,3].
                 … (8 more)

CODE TYPE
  function
    driver: one Solution call per test case

SIGNATURE
  int createSortedArray(vector<int>& instructions);

FILES
  edit : 1649.cpp
  test : lc run 1649
══════════════════════════════════════════════════════════════════════════
