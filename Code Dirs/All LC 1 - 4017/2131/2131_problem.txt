══════════════════════════════════════════════════════════════════════════
 2131. Longest Palindrome by Concatenating Two Letter Words
 Medium  |  Tags: Array, Hash Table, String, Greedy, Counting
 https://leetcode.com/problems/longest-palindrome-by-concatenating-two-letter-words/description/
══════════════════════════════════════════════════════════════════════════

PROBLEM
  You are given an array of strings words. Each element of words consists
  of two lowercase English letters.

  Create the longest possible palindrome by selecting some elements from
  words and concatenating them in any order. Each element can be selected
  at most once.

  Return the length of the longest palindrome that you can create. If it
  is impossible to create any palindrome, return 0.

  A palindrome is a string that reads the same forward and backward.

CONSTRAINTS
  1 <= words.length <= 10^5
  words[i].length == 2
  words[i] consists of lowercase English letters.

TEST CASES
  [1] Input    : words = ["lc","cl","gg"]
      Expected : 6
      Why      : One longest palindrome is "lc" + "gg" + "cl" = "lcggcl", of length 6.
                 Note that "clgglc" is another longest palindrome that can be created.

  [2] Input    : words = ["ab","ty","yt","lc","cl","ab"]
      Expected : 8
      Why      : One longest palindrome is "ty" + "lc" + "cl" + "yt" = "tylcclyt", of length 8.
                 Note that "lcyttycl" is another longest palindrome that can be created.

  [3] Input    : words = ["cc","ll","xx"]
      Expected : 2
      Why      : One longest palindrome is "cc", of length 2.
                 Note that "ll" is another longest palindrome that can be created, and so is "xx".

CODE TYPE
  function
    driver: one Solution call per test case

SIGNATURE
  int longestPalindrome(vector<string>& words);

FILES
  edit : 2131.cpp
  test : lc run 2131
══════════════════════════════════════════════════════════════════════════
