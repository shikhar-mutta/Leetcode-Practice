══════════════════════════════════════════════════════════════════════════
 3532. Path Existence Queries in a Graph I
 Medium  |  Tags: Array, Hash Table, Binary Search, Union-Find, Graph Theory
 https://leetcode.com/problems/path-existence-queries-in-a-graph-i/description/
══════════════════════════════════════════════════════════════════════════

PROBLEM
  You are given an integer n representing the number of nodes in a graph,
  labeled from 0 to n - 1.

  You are also given an integer array nums of length n sorted in non-
  decreasing order, and an integer maxDiff.

  An undirected edge exists between nodes i and j if the absolute
  difference between nums[i] and nums[j] is at most maxDiff (i.e.,
  |nums[i] - nums[j]| <= maxDiff).

  You are also given a 2D integer array queries. For each queries[i] =
  [ui, vi], determine whether there exists a path between nodes ui and vi.

  Return a boolean array answer, where answer[i] is true if there exists a
  path between ui and vi in the i^th query and false otherwise.

CONSTRAINTS
  1 <= n == nums.length <= 10^5
  0 <= nums[i] <= 10^5
  nums is sorted in non-decreasing order.
  0 <= maxDiff <= 10^5
  1 <= queries.length <= 10^5
  queries[i] == [ui, vi]
  0 <= ui, vi < n

TEST CASES
  [1] Input    : n = 2, nums = [1,3], maxDiff = 1, queries = [[0,0],[0,1]]
      Expected : [true,false]
      Why      : - Query [0,0]: Node 0 has a trivial path to itself.
                 - Query [0,1]: There is no edge between Node 0 and Node 1 because |nums[0] - nums[1]| = |1 - 3| = 2, which is greater than maxDiff.
                 - Thus, the final answer after processing all the queries is [true, false].

  [2] Input    : n = 4, nums = [2,5,6,8], maxDiff = 2, queries = [[0,1],[0,2],[1,3],[2,3]]
      Expected : [false,false,true,true]
      Why      : The resulting graph is:
                 - Query [0,1]: There is no edge between Node 0 and Node 1 because |nums[0] - nums[1]| = |2 - 5| = 3, which is greater than maxDiff.
                 - Query [0,2]: There is no edge between Node 0 and Node 2 because |nums[0] - nums[2]| = |2 - 6| = 4, which is greater than maxDiff.
                 … (3 more)

CODE TYPE
  function
    driver: one Solution call per test case

SIGNATURE
  vector<bool> pathExistenceQueries(int n, vector<int>& nums, int maxDiff, vector<vector<int>>& queries);

FILES
  edit : 3532.cpp
  test : lc run 3532
══════════════════════════════════════════════════════════════════════════
