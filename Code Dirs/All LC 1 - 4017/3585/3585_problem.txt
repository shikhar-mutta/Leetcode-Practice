══════════════════════════════════════════════════════════════════════════
 3585. Find Weighted Median Node in Tree
 Hard  |  Tags: Array, Binary Search, Dynamic Programming, Bit Manipulation, Tree, Depth-First Search
 https://leetcode.com/problems/find-weighted-median-node-in-tree/description/
══════════════════════════════════════════════════════════════════════════

PROBLEM
  You are given an integer n and an undirected, weighted tree rooted at
  node 0 with n nodes numbered from 0 to n - 1. This is represented by a
  2D array edges of length n - 1, where edges[i] = [ui, vi, wi] indicates
  an edge from node ui to vi with weight wi.

  The weighted median node is defined as the first node x on the path from
  ui to vi such that the sum of edge weights from ui to x is greater than
  or equal to half of the total path weight.

  You are given a 2D integer array queries. For each queries[j] = [uj,
  vj], determine the weighted median node along the path from uj to vj.

  Return an array ans, where ans[j] is the node index of the weighted
  median for queries[j].

CONSTRAINTS
  2 <= n <= 10^5
  edges.length == n - 1
  edges[i] == [ui, vi, wi]
  0 <= ui, vi < n
  1 <= wi <= 10^9
  1 <= queries.length <= 10^5
  queries[j] == [uj, vj]
  0 <= uj, vj < n
  The input is generated such that edges represents a valid tree.

TEST CASES
  [1] Input    : n = 2, edges = [[0,1,7]], queries = [[1,0],[0,1]]
      Expected : [0,1]
      Why      : Answer
                 [1, 0]
                 1 → 0
                 … (12 more)

  [2] Input    : n = 3, edges = [[0,1,2],[2,0,4]], queries = [[0,1],[2,0],[1,2]]
      Expected : [1,0,2]
      Why      : Answer
                 [0, 1]
                 0 → 1
                 … (20 more)

  [3] Input    : n = 5, edges = [[0,1,2],[0,2,5],[1,3,1],[2,4,3]], queries = [[3,4],[1,2]]
      Expected : [2,2]
      Why      : Answer
                 [3, 4]
                 3 → 1 → 0 → 2 → 4
                 … (15 more)

CODE TYPE
  function
    driver: one Solution call per test case

SIGNATURE
  vector<int> findMedian(int n, vector<vector<int>>& edges, vector<vector<int>>& queries);

FILES
  edit : 3585.cpp
  test : lc run 3585
══════════════════════════════════════════════════════════════════════════
