══════════════════════════════════════════════════════════════════════════
 3715. Sum of Perfect Square Ancestors
 Hard  |  Tags: Array, Hash Table, Math, Tree, Depth-First Search, Counting, Number Theory
 https://leetcode.com/problems/sum-of-perfect-square-ancestors/description/
══════════════════════════════════════════════════════════════════════════

PROBLEM
  You are given an integer n and an undirected tree rooted at node 0 with
  n nodes numbered from 0 to n - 1. This is represented by a 2D array
  edges of length n - 1, where edges[i] = [ui, vi] indicates an undirected
  edge between nodes ui and vi.

  You are also given an integer array nums, where nums[i] is the positive
  integer assigned to node i.

  Define a value ti as the number of ancestors of node i such that the
  product nums[i] * nums[ancestor] is a perfect square.

  Return the sum of all ti values for all nodes i in range [1, n - 1].

  Note:

  - In a rooted tree, the ancestors of node i are all nodes on the path
    from node i to the root node 0, excluding i itself.

CONSTRAINTS
  1 <= n <= 10^5
  edges.length == n - 1
  edges[i] = [ui, vi]
  0 <= ui, vi <= n - 1
  nums.length == n
  1 <= nums[i] <= 10^5
  The input is generated such that edges represents a valid tree.

TEST CASES
  [1] Input    : n = 3, edges = [[0,1],[1,2]], nums = [2,8,2]
      Expected : 3
      Why      : i
                 Ancestors
                 nums[i] * nums[ancestor]
                 … (14 more)

  [2] Input    : n = 3, edges = [[0,1],[0,2]], nums = [1,2,4]
      Expected : 1
      Why      : i
                 Ancestors
                 nums[i] * nums[ancestor]
                 … (13 more)

  [3] Input    : n = 4, edges = [[0,1],[0,2],[1,3]], nums = [1,2,9,4]
      Expected : 2
      Why      : i
                 Ancestors
                 nums[i] * nums[ancestor]
                 … (19 more)

CODE TYPE
  function
    driver: one Solution call per test case

SIGNATURE
  long long sumOfAncestors(int n, vector<vector<int>>& edges, vector<int>& nums);

FILES
  edit : 3715.cpp
  test : lc run 3715
══════════════════════════════════════════════════════════════════════════
