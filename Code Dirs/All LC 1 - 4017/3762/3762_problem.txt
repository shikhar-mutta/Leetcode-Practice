══════════════════════════════════════════════════════════════════════════
 3762. Minimum Operations to Equalize Subarrays
 Hard  |  Tags: Array, Math, Binary Search, Segment Tree, Sorting
 https://leetcode.com/problems/minimum-operations-to-equalize-subarrays/description/
══════════════════════════════════════════════════════════════════════════

PROBLEM
  You are given an integer array nums and an integer k.

  In one operation, you can increase or decrease any element of nums by
  exactly k.

  You are also given a 2D integer array queries, where each queries[i] =
  [li, ri].

  For each query, find the minimum number of operations required to make
  all elements in the subarray nums[li..ri] equal. If it is impossible,
  the answer for that query is -1.

  Return an array ans, where ans[i] is the answer for the i^th query.

CONSTRAINTS
  1 <= n == nums.length <= 4 × 10^4
  1 <= nums[i] <= 10^9
  1 <= k <= 10^9
  1 <= queries.length <= 4 × 10^4
  ^queries[i] = [li, ri]
  0 <= li <= ri <= n - 1

TEST CASES
  [1] Input    : nums = [1,4,7], k = 3, queries = [[0,1],[0,2]]
      Expected : [1,2]
      Why      : One optimal set of operations:
                 i
                 [li, ri]
                 … (22 more)

  [2] Input    : nums = [1,2,4], k = 2, queries = [[0,2],[0,0],[1,2]]
      Expected : [-1,0,1]
      Why      : One optimal set of operations:
                 i
                 [li, ri]
                 … (28 more)

CODE TYPE
  function
    driver: one Solution call per test case

SIGNATURE
  vector<long long> minOperations(vector<int>& nums, int k, vector<vector<int>>& queries);

FILES
  edit : 3762.cpp
  test : lc run 3762
══════════════════════════════════════════════════════════════════════════
